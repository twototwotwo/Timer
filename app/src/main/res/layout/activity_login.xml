
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/APPBcolor1"
    tools:context="com.wangsijiu.app.activity.LoginActivity">

    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/timer_im"
        android:background="@drawable/timer"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"/>
    <!--
<TextView
    android:layout_width="wrap_content"
    android:layout_height="50dp"
    android:text="Timer"
    android:textSize="20dp"
    android:layout_below="@+id/timer_im"
    android:layout_centerHorizontal="true"
    android:gravity="center"/>
<EditText
    android:hint="学号"
    android:inputType="number"
    android:focusable="true"
    android:id="@+id/number"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_marginRight="20dp"
    android:layout_marginLeft="20dp"
    android:layout_marginTop="150dp"
    android:background="@drawable/authorize_button_shape"
    android:layout_centerHorizontal="true"
    />
<EditText
    android:hint="密码"
    android:singleLine="true"
    android:inputType="number"
    android:password="true"
    android:focusable="true"
    android:id="@+id/pass_word"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_marginRight="20dp"
    android:layout_marginLeft="20dp"
    android:layout_marginTop="10dp"
    android:layout_centerHorizontal="true"
    android:background="@drawable/authorize_button_shape"
    android:layout_below="@id/number"
    />
    -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:id="@+id/login_button"
        android:layout_below="@+id/timer_im"
        android:layout_centerHorizontal="true"
        android:background="@drawable/authorize_button_shape"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:textSize="20dp"
        android:textColor="#ffffff"
        android:text="易班授权登录"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:id="@+id/visit_button"
        android:layout_below="@+id/login_button"
        android:layout_centerHorizontal="true"
        android:background="@drawable/authorize_button_shape"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:textSize="20dp"
        android:textColor="#ffffff"
        android:text="游客登录"/>


</RelativeLayout>
